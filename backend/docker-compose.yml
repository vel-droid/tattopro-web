version: "3.9"

services:
  db:
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: pierc_user
      POSTGRES_PASSWORD: pierc_pass
      POSTGRES_DB: pierc_db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
